<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar scroll</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarScroll">
      <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
        <% if user_signed_in? %>
          <% if current_user.has_permission?(:customer_index) %>
            <li class="nav-item">
              <%= link_to t(:customer, scope: %i[activerecord models], count: 2), customers_path, class: 'nav-link' %>
            </li>
          <% end %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarScrollingDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Produtos
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">
              <li><%= link_to t(:list_of_products, scope: %i[_dictionary]), products_path, class: 'dropdown-item' %></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="#">Adicionar Remessa</a></li>
              <li><a class="dropdown-item" href="#">Histórico de Remessas</a></li>
            </ul>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Link</a>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <%= button_to t(:sign_out, scope: %i[devise sessions destroy]), destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
      <% else %>
        <%= button_to t(:sign_in, scope: %i[devise sessions new]), new_user_session_path, class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</nav>
<style>
  .dropdown:hover .dropdown-menu {
    display: block;
    margin-top: 0;
  }
</style>