<!DOCTYPE html>
<html>
  <head>
    <title>ComarSale</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
      <%= link_to t(:sign_out, scope: %i[devise sessions destroy]), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t(:sign_in, scope: %i[devise sessions new]), new_user_session_path %>
    <% end %>
    <% if flash.present? %>
      <% flash.each do |key, value| %>
        <p><%= value %></p>
      <% end %>
    <% end %>
    <%= yield %>
  </body>
</html>
